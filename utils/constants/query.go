package constants

const (
	WHERE_BY_ID         = "id = ?"
	WHERE_BY_USER_ID    = "user_id = ?"
	WHERE_BY_PRODUCT_ID = "id = ?"
	ReceiveProductType  = "receiveProduct"
	SendProductType     = "sendProduct"
)
